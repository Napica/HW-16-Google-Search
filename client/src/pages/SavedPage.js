import React from "react";
import API from "../utils/API";

function savedPages() {

  // need a state/setstate with a useState hook
  
  // need to make the API call from the backend, to pull all the books that were saved into the database
  

  return (
    <div>
      <h1> this will be a saved pages for books</h1>
    </div>
  );
}

export default savedPages;
