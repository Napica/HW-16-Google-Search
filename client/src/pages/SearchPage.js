import React from "react";
import API from "../utils/API";

function searchPages() {

  // Need to add state secions 

  // Need to have an API call to post into setState

  // Need an onclick listener function to create take in the information 
  // once the listener is created, Need a function to call the API to get the queried data.  
  
  return (
    <div>
      <h1> this will be a search pages for books</h1>
    </div>
  );
}

export default searchPages;
